# 💊 新闻胶囊 (News Capsule)

> 像吞服胶囊一样简单，在最短时间内获取不应错过的科技资讯

每日精选高质量科技要闻，3 分钟读完，让你不错过重要信息，又不陷入无限信息流焦虑。

## ✨ 功能特性

- **智能筛选** - AI 从多个信息源中筛选重要的新闻
- **深度摘要** - 每条新闻包含编辑概要、关键要点、原文阅读建议
- **中英双语** - 支持中文和英文两个版本
- **手动管理** - 通过 Admin 后台人工挑选和发布新闻
- **Admin 后台** - 完整的内容管理系统

## 🚀 快速开始

### 1. 安装依赖

```bash
cd news-capsule
npm install
```

### 2. 配置环境变量

复制 `env.example` 为 `.env.local` 并填写：

```bash
cp env.example .env.local
```

```env
# 必需
OPENAI_API_KEY=your_openai_api_key
ADMIN_USERS=admin:your_password

# 云端存储（生产环境必需）
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret
R2_BUCKET_NAME=news-capsule
```

> 详细部署配置请参考 [部署指南](docs/DEPLOYMENT.md)

### 3. 本地运行

```bash
npm run dev
# 访问 http://localhost:3000
```

### 4. 手动生成新闻

```bash
export $(cat .env.local | xargs) && node scripts/generate-news.js zh
```

## 📁 项目结构

```
news-capsule/
├── app/                    # Next.js App Router
│   ├── page.js             # 首页（极简文学风格）
│   ├── admin/              # 管理后台（8个子页面）
│   └── api/                # API 路由
├── components/             # React 组件
│   ├── NewsCardLab.js      # 新闻卡片（极简风格）
│   ├── SubscribeModal.js   # 订阅弹窗
│   └── ...
├── lib/                    # 工具库
│   ├── storage.js          # 存储抽象层（R2/本地）
│   ├── prompts.js          # Prompt 统一管理
│   └── auth.js             # 认证逻辑
├── scripts/
│   └── generate-news.js    # 新闻生成脚本
├── data/                   # 本地数据存储
│   ├── sources.json        # 信息源配置
│   └── feeds/              # 按源分组的新闻数据
└── docs/                   # 文档
```

## 🛠 技术栈

| 技术               | 用途                 |
| ------------------ | -------------------- |
| Next.js 16         | 前端框架 (React 19)  |
| OpenAI GPT-4o-mini | AI 筛选和摘要生成    |
| Cloudflare R2      | 云端存储             |
| Vercel             | 部署 + Cron 定时任务 |

## 📚 文档

| 文档                             | 说明                       |
| -------------------------------- | -------------------------- |
| [技术架构](docs/ARCHITECTURE.md) | 系统概览、数据流、目录结构 |
| [产品需求文档](docs/PRD.md)      | 功能规格、UI 设计          |
| [Admin 功能](docs/admin-prd.md)  | 管理后台详细说明           |
| [部署指南](docs/DEPLOYMENT.md)   | Vercel + R2 部署配置       |

## 📝 License

MIT
